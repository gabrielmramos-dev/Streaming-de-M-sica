package br.com.streaming.modelo;

public class PlaylistPersonalizada extends Playlist {
    
    public PlaylistPersonalizada(String nome) {
        super(nome);
    }
    
    // Herda o comportamento padrão de Playlist (permitindo add e remove)
}
